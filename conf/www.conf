; [www]
; user = www-data
; group = www-data
; listen = 9000

; pm = dynamic
; pm.max_children = 80
; pm.start_servers = 20
; pm.min_spare_servers = 20
; pm.max_spare_servers = 40
; pm.max_requests = 1000

; ; Slowlog to catch long requests
; request_slowlog_timeout = 5s
; slowlog = /var/log/php-fpm/slow.log

; ; Request terminate timeout - 0 = unlimited (needed for large exports)
; request_terminate_timeout = 0

; ; Upload size limits for product images (added 2026-01-16)
; php_admin_value[upload_max_filesize] = 1000M
; php_admin_value[post_max_size] = 11000M
; php_admin_value[memory_limit] = 5120M
; php_admin_value[max_execution_time] = 0
; php_admin_value[max_input_time] = 600
; php_admin_value[max_file_uploads] = 20

[www]
user = www-data
group = www-data
listen = 9000

pm = dynamic
pm.max_children = 50
pm.start_servers = 10
pm.min_spare_servers = 5
pm.max_spare_servers = 20
pm.max_requests = 500
listen.backlog = 256

; Slowlog is good, keep it
request_slowlog_timeout = 5s
slowlog = /var/log/php-fpm/slow.log

; IMPORTANT: Never use 0 (unlimited) in production. 
; 300s (5 minutes) is plenty even for large exports.
request_terminate_timeout = 300s

; Upload size limits
php_admin_value[upload_max_filesize] = 100M
php_admin_value[post_max_size] = 110M
php_admin_value[memory_limit] = 512M
php_admin_value[max_execution_time] = 300
php_admin_value[max_input_time] = 300
php_admin_value[max_file_uploads] = 20